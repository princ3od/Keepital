import 'package:flutter/widgets.dart';

class FinishedBudgetTab extends StatelessWidget {
  const FinishedBudgetTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Finisheds'),
    );
  }
}
